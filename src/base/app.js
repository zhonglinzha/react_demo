import React from 'react';
// import { CSSTransition } from 'react-transition-group';

const App = ({ children }) => <div className='app'>{children}</div>;
// const App = ({ children }) => children.map(i => console.log(i));

export default App;
